//same as set but not sorted only unique
//lower_bound and upper_bound function doesnt work rest all functions are same as others
//stores data in unordered manner, it has better time complexity than set in most cases
#include<bits/stdc++.h>

int main(void){
	std::unordered_set<int> uo;
	return 0;
}
